<h1>Photosets</h1>
<ul>
  <%= link_to "New", photoset_new_url %>
  <% @photosets.each do |set| %>
  <li><%= link_to set.name, { :controller => :image, :action => :upload, :photoset => set } %> - <%= set.pictures.size %> pictures</li>
  <% end %>
</ul>
